2025年-11月-08日：完成 Qt5+osgEarth+OpenCV 项目骨架，搭建 Qt 主窗口与三维视图框架，并配置 CMake。
2025年-11月-08日：调研参考项目 OsgEarthQt-main，形成整合方案，明确场景控件、交互工具、态势组件与资源配置路径。
2025年-11月-08日：复用参考项目 UI，接入 Qt Designer 布局、图标资源与动作骨架，完成三维仿真主窗口搭建。
2025年-11月-08日：将图标等静态资源统一迁移至 resource 目录，并更新 QRC 与 CMake 引用。
2025年-11月-08日：在 CMake 中打印 Qt、OpenCV、OpenSceneGraph、osgEarth 等依赖的定位路径，便于环境排查。
2025年-11月-08日：自动为 OpenCV 和 OpenSceneGraph 创建命名空间 IMPORTED 目标，保证不同安装方式下的链接一致性。
2025年-11月-08日：对 osg::Box 引用改为包含 osg/Shape，兼容新版 OSG 头文件布局，解决编译缺失。
2025年-11月-08日：为 SceneWidget 提供全局别名，确保 Qt Designer 自动生成代码能识别自定义控件类型，修复构建错误。
2025年-11月-08日：使用 osgViewer::GraphicsWindowEmbedded 重构 SceneWidget，在 Qt 单窗口内嵌 osgEarth 视图并移除 osgQt 依赖。
2025年-11月-08日：在 SceneWidget 中调用 osgEarth::initialize，修复嵌入视图初始化警告。
2025年-11月-08日：修复界面图标不显示问题，在main.cpp中添加Q_INIT_RESOURCE(ui_resources)初始化资源文件，并调整CMakeLists.txt将资源文件正确链接到可执行文件。
2025年-11月-08日：实现文件-地球打开.earth文件功能，添加文件选择对话框，使用osgDB读取.earth文件并加载到osgEarth场景中。
2025年-11月-08日：移植视图交互与经纬度显示：接入 EarthManipulator，桥接 Qt 鼠标/键盘/滚轮事件到 osgViewer（SceneWidget），实现窗口像素坐标到地理坐标拾取（computeGeoAt，使用 LineSegmentIntersector + SRS 转换），并在主窗口状态栏新增经纬度/高程实时显示；加载 .earth 后自动回到 Home 视点。
2025年-11月-08日：交互优化与问题修复：统一高DPI设备像素坐标，修正Qt顶左到OSG窗坐标映射；右键拖拽缩放改为分步滚轮事件以避免跳变；键盘方向键改为合成小幅鼠标拖拽（2px）消除每次首按跳步；PageUp/PageDown 映射为滚轮缩放；初始化视图中心并同步 EventQueue 窗口矩形与指针位置；增加调试日志辅助定位。
